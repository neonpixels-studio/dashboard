# No runtime env vars are wired in yet. As you add services (e.g. a database or
# auth), add their vars here, then: copy this file to .env, fill in real values,
# and encrypt with `npx dotenvx encrypt` (generates .env.keys — do NOT commit it).
