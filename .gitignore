.DS_Store
node_modules/
.nuxt/
.output/
dist/
coverage/
.fallow/
e2e/artifacts/
e2e/report/
/.netlify
/.claude

# Local env (dotenvx). The encrypted .env* files ARE committed; the private
# keys and any local-only plaintext overrides are NOT.
.env.keys
.env.local
.env.*.local
